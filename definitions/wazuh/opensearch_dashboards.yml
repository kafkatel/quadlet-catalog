# opensearch_dashboards.yml
# Wazuh Dashboard configuration.
# Copy to /srv/containers/wazuh/config/opensearch_dashboards.yml before deploying.
# Adjust indexer credentials and hostnames to match your deployment.

server.host: "0.0.0.0"
server.port: 5601

# Indexer connection
opensearch.hosts: ["https://wazuh-indexer:9200"]
opensearch.username: "kibanaserver"
opensearch.password: "kibanaserver"

# TLS — server-side (dashboard HTTPS)
server.ssl.enabled: true
server.ssl.key: "/usr/share/wazuh-dashboard/certs/wazuh-dashboard-key.pem"
server.ssl.certificate: "/usr/share/wazuh-dashboard/certs/wazuh-dashboard.pem"

# TLS — client-side (dashboard → indexer)
opensearch.ssl.certificateAuthorities: ["/usr/share/wazuh-dashboard/certs/root-ca.pem"]
opensearch.ssl.verificationMode: certificate

# Security plugin
opensearch.requestHeadersAllowlist: ["securitytenant", "Authorization"]
opensearch_security.multitenancy.enabled: false
opensearch_security.readonly_mode.roles: ["kibana_read_only"]

# Wazuh plugin
uiSettings.overrides.defaultRoute: /app/wazuh
